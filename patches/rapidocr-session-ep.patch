diff --git a/include/AngleNet.h b/include/AngleNet.h
index d14503c..bb7f7a7 100644
--- a/include/AngleNet.h
+++ b/include/AngleNet.h
@@ -22,7 +22,9 @@ public:
 private:
     bool isOutputAngleImg = false;
 
-    Ort::Session *session;
+    // gRParse: initialized so a Session construction failure leaves a defined
+    // value for the destructor (patches/rapidocr-session-ep.patch).
+    Ort::Session *session = nullptr;
     Ort::Env env = Ort::Env(ORT_LOGGING_LEVEL_ERROR, "AngleNet");
     Ort::SessionOptions sessionOptions = Ort::SessionOptions();
     int numThread = 0;
diff --git a/include/CrnnNet.h b/include/CrnnNet.h
index 2b4cdbe..86ec3d4 100644
--- a/include/CrnnNet.h
+++ b/include/CrnnNet.h
@@ -20,7 +20,9 @@ public:
 
 private:
     bool isOutputDebugImg = false;
-    Ort::Session *session;
+    // gRParse: initialized so a Session construction failure leaves a defined
+    // value for the destructor (patches/rapidocr-session-ep.patch).
+    Ort::Session *session = nullptr;
     Ort::Env env = Ort::Env(ORT_LOGGING_LEVEL_ERROR, "CrnnNet");
     Ort::SessionOptions sessionOptions = Ort::SessionOptions();
     int numThread = 0;
diff --git a/include/DbNet.h b/include/DbNet.h
index e2b9a17..e654fc6 100644
--- a/include/DbNet.h
+++ b/include/DbNet.h
@@ -19,7 +19,9 @@ public:
                                       float boxThresh, float unClipRatio);
 
 private:
-    Ort::Session *session;
+    // gRParse: initialized so a Session construction failure leaves a defined
+    // value for the destructor (patches/rapidocr-session-ep.patch).
+    Ort::Session *session = nullptr;
     Ort::Env env = Ort::Env(ORT_LOGGING_LEVEL_ERROR, "DbNet");
     Ort::SessionOptions sessionOptions = Ort::SessionOptions();
     int numThread = 0;
diff --git a/src/AngleNet.cpp b/src/AngleNet.cpp
index 437fb86..5bbc9f7 100644
--- a/src/AngleNet.cpp
+++ b/src/AngleNet.cpp
@@ -1,24 +1,12 @@
 #include "AngleNet.h"
 #include "OcrUtils.h"
+#include "grparse_session_ep.h"
 #include <numeric>
 
 void AngleNet::setGpuIndex(int gpuIndex) {
-#ifdef __CUDA__
-    if (gpuIndex >= 0) {
-        OrtCUDAProviderOptions cuda_options;
-        cuda_options.device_id = gpuIndex;
-        cuda_options.arena_extend_strategy = 0;
-        cuda_options.gpu_mem_limit = 2 * 1024 * 1024 * 1024;
-        cuda_options.cudnn_conv_algo_search = OrtCudnnConvAlgoSearch::OrtCudnnConvAlgoSearchExhaustive;
-        cuda_options.do_copy_in_default_stream = 1;
-
-        sessionOptions.AppendExecutionProvider_CUDA(cuda_options);
-        printf("cls try to use GPU%d\n", gpuIndex);
-    }
-    else {
-        printf("cls use CPU\n");
-    }
-#endif
+    // gRParse: execution-provider selection is centralized so CUDA, OpenVINO,
+    // and CPU builds share one decision point (see grparse_session_ep.h).
+    grparse::append_execution_provider(sessionOptions, gpuIndex);
 }
 
 AngleNet::~AngleNet() {
@@ -49,6 +37,10 @@ void AngleNet::setNumThread(int numOfThread) {
 }
 
 void AngleNet::initModel(const std::string &pathStr) {
+    // gRParse: OpenVINO compiles the model inside the Session ctor, and the
+    // toolchain's JIT compiler is not safe under concurrent compiles from a
+    // cold kernel cache; the gate serializes session builds process-wide.
+    grparse::OvCompileGate grparseCompileGate;
 #ifdef _WIN32
     std::wstring clsPath = strToWstr(pathStr);
     session = new Ort::Session(env, clsPath.c_str(), sessionOptions);
diff --git a/src/CrnnNet.cpp b/src/CrnnNet.cpp
index a3a5d3a..1b2d782 100644
--- a/src/CrnnNet.cpp
+++ b/src/CrnnNet.cpp
@@ -1,25 +1,13 @@
 #include "CrnnNet.h"
 #include "OcrUtils.h"
+#include "grparse_session_ep.h"
 #include <fstream>
 #include <numeric>
 
 void CrnnNet::setGpuIndex(int gpuIndex) {
-#ifdef __CUDA__
-    if (gpuIndex >= 0) {
-        OrtCUDAProviderOptions cuda_options;
-        cuda_options.device_id = gpuIndex;
-        cuda_options.arena_extend_strategy = 0;
-        cuda_options.gpu_mem_limit = 2 * 1024 * 1024 * 1024;
-        cuda_options.cudnn_conv_algo_search = OrtCudnnConvAlgoSearch::OrtCudnnConvAlgoSearchExhaustive;
-        cuda_options.do_copy_in_default_stream = 1;
-
-        sessionOptions.AppendExecutionProvider_CUDA(cuda_options);
-        printf("rec try to use GPU%d\n", gpuIndex);
-}
-    else {
-        printf("rec use CPU\n");
-    }
-#endif
+    // gRParse: execution-provider selection is centralized so CUDA, OpenVINO,
+    // and CPU builds share one decision point (see grparse_session_ep.h).
+    grparse::append_execution_provider(sessionOptions, gpuIndex);
 }
 
 CrnnNet::~CrnnNet() {
@@ -50,6 +38,10 @@ void CrnnNet::setNumThread(int numOfThread) {
 }
 
 void CrnnNet::initModel(const std::string &pathStr, const std::string &keysPath) {
+    // gRParse: OpenVINO compiles the model inside the Session ctor, and the
+    // toolchain's JIT compiler is not safe under concurrent compiles from a
+    // cold kernel cache; the gate serializes session builds process-wide.
+    grparse::OvCompileGate grparseCompileGate;
 #ifdef _WIN32
     std::wstring crnnPath = strToWstr(pathStr);
     session = new Ort::Session(env, crnnPath.c_str(), sessionOptions);
diff --git a/src/DbNet.cpp b/src/DbNet.cpp
index 74b93aa..cc92b7e 100644
--- a/src/DbNet.cpp
+++ b/src/DbNet.cpp
@@ -1,23 +1,11 @@
 #include "DbNet.h"
 #include "OcrUtils.h"
+#include "grparse_session_ep.h"
 
 void DbNet::setGpuIndex(int gpuIndex) {
-#ifdef __CUDA__
-    if (gpuIndex >= 0) {
-        OrtCUDAProviderOptions cuda_options;
-        cuda_options.device_id = gpuIndex;
-        cuda_options.arena_extend_strategy = 0;
-        cuda_options.gpu_mem_limit = 2 * 1024 * 1024 * 1024;
-        cuda_options.cudnn_conv_algo_search = OrtCudnnConvAlgoSearch::OrtCudnnConvAlgoSearchExhaustive;
-        cuda_options.do_copy_in_default_stream = 1;
-
-        sessionOptions.AppendExecutionProvider_CUDA(cuda_options);
-        printf("det try to use GPU%d\n", gpuIndex);
-    }
-    else {
-        printf("det use CPU\n");
-    }
-#endif
+    // gRParse: execution-provider selection is centralized so CUDA, OpenVINO,
+    // and CPU builds share one decision point (see grparse_session_ep.h).
+    grparse::append_execution_provider(sessionOptions, gpuIndex);
 }
 
 DbNet::~DbNet() {
@@ -48,6 +36,10 @@ void DbNet::setNumThread(int numOfThread) {
 }
 
 void DbNet::initModel(const std::string &pathStr) {
+    // gRParse: OpenVINO compiles the model inside the Session ctor, and the
+    // toolchain's JIT compiler is not safe under concurrent compiles from a
+    // cold kernel cache; the gate serializes session builds process-wide.
+    grparse::OvCompileGate grparseCompileGate;
 #ifdef _WIN32
     std::wstring detPath = strToWstr(pathStr);
     session = new Ort::Session(env, detPath.c_str(), sessionOptions);
