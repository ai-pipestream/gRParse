services:
  grparse:
    build: .
    ports:
      - "50051:50051"
    volumes:
      - ./models:/models:ro
    environment:
      CUDA_CACHE_DISABLE: "1"
    gpus: all
    read_only: true
    tmpfs:
      - /tmp:size=256m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
